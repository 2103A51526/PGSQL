-- 1. WQTD all the emp from emp table.
select * from emp;

-- 2. WQTD to display the empno, ename, job, mgr, hiredate, sal, comm, deptno from emp table.
select empno, ename, job, manager, hiredate, sal, comm, deptno from emp;

